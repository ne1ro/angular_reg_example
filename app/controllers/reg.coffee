# Registration and authentication controller
class RegCtrl extends BaseCtrl
  @register '$scope', '$state'

  # On controller init
  initialize: =>

  # Sign up new user
  signUp: (data) =>

  # Log into the site
  logIn: (data) =>
